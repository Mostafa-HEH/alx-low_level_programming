#ifndef MAIN_H
#define MAIN_H
#include <stdlib.h>

/*
 * Structs, enums and unions definitions
 * Typedefs
 * Function prototypes
 */
char *create_array(unsigned int size, char c);
char *_strdup(char *str);

#endif /* MAIN_H */
